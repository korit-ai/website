---
title: "Example post title"
description: "One-sentence summary shown on the listing page — keep it short."
pubDate: 2026-08-09
locale: "en"
draft: true
---

This is a template post, not a real one — `draft: true` keeps it out of the
listing page, the individual post route, and the RSS feed.

To publish a real post: copy this file, write it in English or Turkish (set
`locale` accordingly — each locale's listing page only shows its own posts,
so a post needs one file per language if it should appear in both), and set
`draft: false`. See `kb/project/content-inventory.md` for the full frontmatter
reference.
